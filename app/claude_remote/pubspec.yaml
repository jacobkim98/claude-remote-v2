name: claude_remote
description: "Claude Remote Control - Control Claude Code from your phone"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  web_socket_channel: ^2.4.0
  flutter_local_notifications: ^18.0.0
  shared_preferences: ^2.3.0
  wakelock_plus: ^1.2.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
